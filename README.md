This tool can extract data from a video file containing screenshots with raw binary data in them. It was originally written for dumping the ModRetro N64 games, but it can probably be used for other stuff too.

It doesn't currently have a nice interface, so to use it you'll have to edit the source code to fill in your own paths. 

# Licence

The ffmpeg code is largely based on [dump_frames.rs](https://github.com/zmwangx/rust-ffmpeg/blob/master/examples/dump-frames.rs), which is distributed under the Do What The F*ck You Want To Public License, and so I've chosen to copy that.
